export declare enum PrismaBlockType {
    GENERATOR = "generator",
    DATA_SOURCE = "datasource",
    MODEL = "model",
    ENUM = "enum"
}
export declare type PrismaBlockName = `${PrismaBlockType}`;
